import styled from "styled-components";
import FlexContainer from "./flexContainer";

const InnerContainers = styled(FlexContainer)`
  height: 100vh;
  display: flex;
`;

export default InnerContainers;
