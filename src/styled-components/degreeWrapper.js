import styled from "styled-components";

const DegreeWrapper = styled.span`
  color: var(--tx-primary);
  font-size: 143px;
  font-weight: 500;
  text-align: end;
`;
// original 144

export default DegreeWrapper;
