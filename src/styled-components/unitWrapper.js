import styled from "styled-components";
import DegreeWrapper from "./degreeWrapper";

const UnitWrapper = styled(DegreeWrapper)`
  color: var(--tx-secondary);
  font-size: 48px;
  font-weight: 100;
  text-align: start;
`;

export default UnitWrapper;
